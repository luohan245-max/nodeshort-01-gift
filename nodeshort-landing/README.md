# NodeShort Landing (Facebook / Vercel)

- Static site: `index.html` + `/assets/`.
- Optimized PNG assets sized for fast load (768px UI screenshots, 256px icons).
- Facebook Pixel ID included in `<head>`.
- All CTAs point to your URL.
